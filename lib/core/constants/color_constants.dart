import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF212121); // Koyu arka plan
  static const Color primary = Color(0xFF1E88E5); // Ana mavi renk
  static const Color accent = Color(0xFFFFC107); // Sarı vurgu rengi
  static const Color textPrimary = Colors.white; // Birincil metin rengi
  static const Color textSecondary = Colors.white70; // İkincil metin rengi
  static const Color divider =
      Color(0xFF616161); // Bölme çizgileri için gri renk
  static Color buttonBackground = Colors.black54; // Butonların arka plan rengi
  static const Color buttonText = Colors.white; // Buton yazı rengi
  static Color entryBackgroundColor = Colors.grey.shade800;
}
